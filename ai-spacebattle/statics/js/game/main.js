
function init () {  
  const game = new Game('game', CONSTANTS.WIDTH, CONSTANTS.HEIGHT)
}

function sqr(a) {
  return a*a;
}

function distance(x1, y1, x2, y2) {
  return Math.sqrt(sqr(y2 - y1) + sqr(x2 - x1));
}
